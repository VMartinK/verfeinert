# Scripts

This directory contains maintainer scripts for the Verfeinert repository.

Scripts must use public package interfaces, avoid generated outputs as source,
and keep any validation artifacts under caller-provided output roots.

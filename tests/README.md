# Tests

This directory will contain tests for the future Verfeinert framework.

The skeleton contains no migrated tests yet. Future default tests should avoid
notebook execution, QNode execution, experiments, plotting, and generated
outputs.
